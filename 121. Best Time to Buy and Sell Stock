class Solution {
    public int maxProfit(int[] prices) {
        int min = Integer.MAX_VALUE;
        int m = 0;
        for (int price : prices) {
            if (price < min) {
                min = price;
            } else if (price - min > m) {
                m = price - min;
            }
        }
        return m;
    }
}
